/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  experimental: {
    // any experimentals if needed, but standard should be fine
  }
};

export default nextConfig;
